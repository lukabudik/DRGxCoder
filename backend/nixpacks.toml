[phases.setup]
nixPkgs = ["python313"]

[phases.install]
cmds = ["uv sync --locked --no-dev --no-install-project"]

[phases.build]
cmds = [
  "prisma generate",
  "prisma py fetch"
]

[start]
cmd = "uvicorn app.main:app --host 0.0.0.0 --port $PORT"
